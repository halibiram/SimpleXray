name: "Auto Release"

on:
  push:
    branches:
      - main

jobs:
  build-and-release:
    name: Build and Auto Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: Explicit Submodule Update
        run: |
          git submodule sync --recursive
          git submodule update --init --recursive

      - name: Read Versions
        id: version
        run: |
          VERSION_NAME=$(grep 'APP_VERSION_NAME' version.properties | cut -d '=' -f 2)
          VERSION_CODE=$(grep 'APP_VERSION_CODE' version.properties | cut -d '=' -f 2)
          echo "version_name=$VERSION_NAME" >> $GITHUB_OUTPUT
          echo "version_code=$VERSION_CODE" >> $GITHUB_OUTPUT
          echo "tag=v$VERSION_NAME" >> $GITHUB_OUTPUT

          echo "XRAY_VERSION=$(grep 'XRAY_CORE_VERSION' version.properties | cut -d '=' -f 2)" >> $GITHUB_ENV
          echo "GEOIP_VERSION=$(grep 'GEOIP_VERSION' version.properties | cut -d '=' -f 2)" >> $GITHUB_ENV
          echo "GEOSITE_VERSION=$(grep 'GEOSITE_VERSION' version.properties | cut -d '=' -f 2)" >> $GITHUB_ENV
          echo "GO_VERSION=$(grep 'GO_VERSION' version.properties | cut -d '=' -f 2)" >> $GITHUB_ENV
          # OPENSSL_VERSION no longer needed - using BoringSSL

      - name: Check if tag exists
        id: check_tag
        run: |
          if git ls-remote --tags origin | grep -q "refs/tags/v${{ steps.version.outputs.version_name }}$"; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "⚠️  Tag v${{ steps.version.outputs.version_name }} already exists, skipping release"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "✅ Tag v${{ steps.version.outputs.version_name }} does not exist, proceeding with build"
          fi

      - name: Set up Java 17
        if: steps.check_tag.outputs.exists == 'false'
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"
          cache: "gradle"

      - name: Set up Android SDK
        if: steps.check_tag.outputs.exists == 'false'
        uses: android-actions/setup-android@v3

      - name: Set up Go
        if: steps.check_tag.outputs.exists == 'false'
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Setup Keystore and Properties
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          echo "${{ secrets.SIGNING_KEYSTORE }}" > keystore.b64
          base64 -d keystore.b64 > ./store.jks
          rm keystore.b64
          echo "storeFile=store.jks" > ./store.properties
          echo "storePassword=${{ secrets.SIGNING_STORE_PASSWORD }}" >> ./store.properties
          echo "keyAlias=${{ secrets.SIGNING_KEY_ALIAS }}" >> ./store.properties
          echo "keyPassword=${{ secrets.SIGNING_KEY_PASSWORD }}" >> ./store.properties

      - name: Download Rules Files
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          wget https://github.com/lhear/v2ray-rules-dat/releases/download/${{ env.GEOIP_VERSION }}/geoip.dat -O ./app/src/main/assets/geoip.dat
          wget https://github.com/lhear/v2ray-rules-dat/releases/download/${{ env.GEOSITE_VERSION }}/geosite.dat -O ./app/src/main/assets/geosite.dat

      - name: Build Xray-core from Source
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          wget -qO android-ndk.zip https://dl.google.com/android/repository/android-ndk-r28c-linux.zip
          unzip -q android-ndk.zip
          rm android-ndk.zip
          NDK_HOME=$(realpath android-ndk-*)

          git clone https://github.com/XTLS/Xray-core.git
          cd Xray-core
          git checkout ${{ env.XRAY_VERSION }}
          COMMID=$(git rev-parse HEAD | cut -c 1-7)

          export GOOS=android
          export CGO_ENABLED=1

          echo "Building for arm64-v8a..."
          export GOARCH=arm64
          export CC=$NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android24-clang
          go build -o xray -trimpath -buildvcs=false -ldflags="-X github.com/xtls/xray-core/core.build=${COMMID} -s -w -buildid=" -v ./main
          mkdir -p ../app/src/main/jniLibs/arm64-v8a
          mv xray ../app/src/main/jniLibs/arm64-v8a/libxray.so

          echo "Building for x86_64..."
          export GOARCH=amd64
          export CC=$NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/x86_64-linux-android24-clang
          go build -o xray -trimpath -buildvcs=false -ldflags="-X github.com/xtls/xray-core/core.build=${COMMID} -s -w -buildid=" -v ./main
          mkdir -p ../app/src/main/jniLibs/x86_64
          mv xray ../app/src/main/jniLibs/x86_64/libxray.so

      - name: Build BoringSSL from Submodule
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          # NDK should already be available from Xray-core build step
          # Find NDK directory (should exist from previous step)
          if [ -z "$(ls -d android-ndk-* 2>/dev/null)" ]; then
            echo "NDK not found, downloading..."
            wget -qO android-ndk.zip https://dl.google.com/android/repository/android-ndk-r28c-linux.zip
            unzip -q android-ndk.zip
            rm android-ndk.zip
          fi
          NDK_HOME=$(realpath android-ndk-*)
          echo "Using NDK: $NDK_HOME"

          # Install build tools
          sudo apt-get update
          sudo apt-get install -y ninja-build clang lld cmake

          # BoringSSL is already a git submodule, ensure it's initialized
          BORINGSSL_DIR="app/src/main/jni/perf-net/third_party/boringssl"
          if [ ! -d "$BORINGSSL_DIR" ]; then
            echo "❌ BoringSSL submodule not found!"
            echo "Please ensure BoringSSL is initialized as a git submodule:"
            echo "  git submodule update --init --recursive"
            exit 1
          fi

          cd "$BORINGSSL_DIR"

          # Function to build BoringSSL for a specific ABI using CMake
          build_boringssl() {
            local OUTPUT_ARCH=$1
            local TOOLCHAIN_PREFIX=$2
            local CMAKE_ARCH=$3

            echo "Building BoringSSL for $OUTPUT_ARCH..."

            # Use modern NDK toolchain directly
            local TOOLCHAIN_DIR="$NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64"
            export PATH=$TOOLCHAIN_DIR/bin:$PATH
            export CC=$TOOLCHAIN_PREFIX-clang
            export CXX=$TOOLCHAIN_PREFIX-clang++
            export AR=$TOOLCHAIN_DIR/bin/llvm-ar
            export RANLIB=$TOOLCHAIN_DIR/bin/llvm-ranlib
            export STRIP=$TOOLCHAIN_DIR/bin/llvm-strip
            export ANDROID_NDK_HOME=$NDK_HOME
            export ANDROID_NDK_ROOT=$NDK_HOME

            # Create build directory
            BUILD_DIR="build_$OUTPUT_ARCH"
            mkdir -p "$BUILD_DIR"
            cd "$BUILD_DIR"

            # Configure BoringSSL with CMake
            cmake .. \
              -DCMAKE_SYSTEM_NAME=Android \
              -DCMAKE_SYSTEM_VERSION=24 \
              -DCMAKE_ANDROID_ARCH_ABI=$CMAKE_ARCH \
              -DCMAKE_ANDROID_NDK=$NDK_HOME \
              -DCMAKE_TOOLCHAIN_FILE=$NDK_HOME/build/cmake/android.toolchain.cmake \
              -DCMAKE_BUILD_TYPE=Release \
              -DOPENSSL_SMALL=1 \
              -DOPENSSL_NO_DEPRECATED=1 \
              -DOPENSSL_NO_ASM=0 \
              -DBUILD_SHARED_LIBS=OFF \
              -GNinja

            # Build
            ninja -j$(nproc)

            # Copy libraries (BoringSSL creates libcrypto.a and libssl.a)
            # These are automatically linked by CMake in the main build
            cd ..

            echo "✅ BoringSSL build complete for $OUTPUT_ARCH"
          }

          # Build for arm64-v8a only (optimized for mobile, matching Xray-core build)
          build_boringssl arm64-v8a "aarch64-linux-android24" "arm64-v8a"

          cd ../../../../..
          echo "✅ BoringSSL build complete"

      - name: Make gradlew executable
        if: steps.check_tag.outputs.exists == 'false'
        run: chmod +x ./gradlew

      - name: Setup Google Services JSON
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          # Ensure google-services.json exists in the app directory
          if [ -f "./app/google-services.json" ]; then
            echo "✅ google-services.json found at app/google-services.json"
            # Also copy to variant-specific location for release build
            mkdir -p ./app/src/release
            cp ./app/google-services.json ./app/src/release/google-services.json
            echo "✅ Copied google-services.json to app/src/release/"
          else
            echo "⚠️  google-services.json not found, creating a minimal template"
            mkdir -p ./app/src/release
            printf '%s\n' \
              '{' \
              '  "project_info": {' \
              '    "project_number": "123456789000",' \
              '    "project_id": "simplexray-template",' \
              '    "storage_bucket": "simplexray-template.appspot.com"' \
              '  },' \
              '  "client": [' \
              '    {' \
              '      "client_info": {' \
              '        "mobilesdk_app_id": "1:123456789000:android:0000000000000000000000",' \
              '        "android_client_info": {' \
              '          "package_name": "com.simplexray.an"' \
              '        }' \
              '      },' \
              '      "oauth_client": [],' \
              '      "api_key": [' \
              '        {' \
              '          "current_key": "AIzaSyDummy-Key-Replace-With-Real-Firebase-Key"' \
              '        }' \
              '      ],' \
              '      "services": {' \
              '        "appinvite_service": {' \
              '          "other_platform_oauth_client": []' \
              '        }' \
              '      }' \
              '    }' \
              '  ],' \
              '  "configuration_version": "1"' \
              '}' > ./app/src/release/google-services.json
            echo "✅ Created minimal google-services.json template"
          fi

      - name: Build Release APK
        if: steps.check_tag.outputs.exists == 'false'
        run: ./gradlew assembleRelease

      - name: Find and Copy APKs
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          mkdir -p release-artifacts
          find app/build -name "*.apk" -type f -exec cp {} release-artifacts/ \;
          echo "=== APKs copied to release-artifacts/ ==="
          ls -lah release-artifacts/

      - name: Create Release
        if: steps.check_tag.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="v${{ steps.version.outputs.version_name }}"
          VERSION="${{ steps.version.outputs.version_name }}"
          BUILD="${{ steps.version.outputs.version_code }}"
          COMMIT="${{ github.sha }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "$TAG" -m "Release $TAG"
          git push origin "$TAG"

          cat > release_notes.md << EOF
          🚀 Automatic release for version ${VERSION}

          **Build Information:**
          - Version: ${VERSION}
          - Build Code: ${BUILD}
          - Commit: ${COMMIT}

          **Download APKs:**
          - \`simplexray-arm64-v8a.apk\` - For most modern devices (ARM64) - BoringSSL optimized

          **Features:**
          - 🔒 BoringSSL integration (replaces OpenSSL)
          - ⚡ Hardware-accelerated crypto (AES-GCM, ChaCha20-Poly1305)
          - 🚀 TLS 1.3 with Chrome mobile fingerprint mimic
          - 📡 QUIC/HTTP3 support

          **Changes:**
          See commit history for detailed changes.

          ---
          🤖 Auto-generated release
          EOF

          # Only upload arm64-v8a APK (BoringSSL optimized for ARM64)
          if [ -f "release-artifacts/simplexray-arm64-v8a.apk" ]; then
            gh release create "$TAG" \
              --title "SimpleXray $TAG" \
              --notes-file release_notes.md \
              release-artifacts/simplexray-arm64-v8a.apk
          else
            echo "⚠️  APK not found, creating release without APK"
            gh release create "$TAG" \
              --title "SimpleXray $TAG" \
              --notes-file release_notes.md
          fi

      - name: Send Release Notification to Telegram
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          BOT_TOKEN="${{ secrets.TELEGRAM_BOT_TOKEN }}"
          CHAT_ID="${{ secrets.TELEGRAM_CHAT_ID }}"

          if [ -z "$BOT_TOKEN" ] || [ -z "$CHAT_ID" ]; then
            echo "⚠️  Telegram secrets not configured"
            exit 0
          fi

          TAG="v${{ steps.version.outputs.version_name }}"
          RELEASE_URL="https://github.com/${{ github.repository }}/releases/tag/$TAG"

          TEXT="🎉 *New SimpleXray Release*\\n\\n📦 *Version:* $TAG\\n🔨 *Build:* ${{ steps.version.outputs.version_code }}\\n🔗 [Download Now]($RELEASE_URL)\\n\\n✨ Auto-update available in app!"

          curl -s -X POST \
            -H "Content-Type: application/json" \
            -d "{\"chat_id\": \"$CHAT_ID\", \"text\": \"$TEXT\", \"parse_mode\": \"Markdown\"}" \
            "https://api.telegram.org/bot$BOT_TOKEN/sendMessage"
